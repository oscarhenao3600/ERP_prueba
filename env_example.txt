# Variables de entorno para el sistema ERP de gestión de documentos

# Base de datos PostgreSQL
DATABASE_URL=postgresql://erp_user:erp_password@localhost:5432/erp_documents

# Django Configuration
SECRET_KEY=django-insecure-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Cloud Storage - Amazon S3
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_STORAGE_BUCKET_NAME=erp-documents-bucket
AWS_S3_REGION_NAME=us-east-1
AWS_S3_CUSTOM_DOMAIN=
AWS_S3_FILE_OVERWRITE=False
AWS_DEFAULT_ACL=None
AWS_S3_VERIFY=True

# Cloud Storage - Google Cloud Storage (alternativa)
# GOOGLE_CLOUD_PROJECT_ID=your_project_id
# GOOGLE_CLOUD_CREDENTIALS_FILE=path/to/credentials.json
# GOOGLE_CLOUD_BUCKET_NAME=erp-documents-bucket

# Configuración de URLs pre-firmadas
PRESIGNED_URL_EXPIRATION=3600  # 1 hora en segundos
MAX_FILE_SIZE=10485760  # 10MB en bytes
ALLOWED_MIME_TYPES=application/pdf,image/jpeg,image/png,image/gif,application/msword,application/vnd.openxmlformats-officedocument.wordprocessingml.document

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=True

# Email Configuration (opcional)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your_email@gmail.com
EMAIL_HOST_PASSWORD=your_app_password

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/erp_documents.log

# Cache (opcional)
CACHE_URL=redis://localhost:6379/1

# Celery (opcional para tareas asíncronas)
CELERY_BROKER_URL=redis://localhost:6379/0
CELERY_RESULT_BACKEND=redis://localhost:6379/0
